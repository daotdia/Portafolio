{
  "devDependencies": {
    "autoprefixer": "^10.4.14",
    "compass": "^0.1.1",
    "compass-importer": "^0.4.1",
    "postcss": "^8.4.23",
    "postcss-cli": "^10.1.0",
    "tailwindcss": "^3.3.2"
  },
  "scripts": {
    "build:compass": "compass compile",
    "build:tailwind": "tailwindcss build src/styles.css -o dist/styles.css",
    "build": "npm run build:compass && npm run build:tailwind"
  }
}
